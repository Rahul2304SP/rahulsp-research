node_modules/
.next/
out/
.env.local
.env
*.tsbuildinfo
# Cloudflare local secrets for `wrangler pages dev` — NEVER commit (holds the
# HomeFinder passkey + write key). Production secrets live in Cloudflare env.
.dev.vars
